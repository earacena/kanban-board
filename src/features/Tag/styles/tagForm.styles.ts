import { css } from '@emotion/react';

export const tagFormStyle = css({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
});

export const textInputStyle = css({
  marginBottom: '1rem',
});

export const colorInputStyle = css({
  marginBottom: '1rem',
});
