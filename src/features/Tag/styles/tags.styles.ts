import { css } from '@emotion/react';

export const tagStyle = css({
  margin: '5px',
  cursor: 'pointer',
});

export const tagContainerStyle = css({
  display: 'flex',
  alignItems: 'center',
  flexWrap: 'wrap',
  marginLeft: '10px',
});
